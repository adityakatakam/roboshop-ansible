#dnf install maven -y
#cp shipping.service /etc/systemd/system/shipping.service
#useradd roboshop
#mkdir /app
#curl -L -o /tmp/shipping.zip https://roboshop-artifacts.s3.amazonaws.com/shipping-v3.zip
#cd /app
#unzip /tmp/shipping.zip
#cd /app
#mvn clean package
#mv target/shipping-1.0.jar shipping.jar
#systemctl daemon-reload
#systemctl enable shipping
#systemctl start shipping
#dnf install mysql -y
#mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/schema.sql
#mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/app-user.sql
#mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/master-data.sql
#
#systemctl restart shipping

- name: Shipping setup
  hosts: all
  become: true
  tasks:
    - name: Install Maven
      ansible.builtin.dnf:
        name: 'maven'
        state: present

    - name: Copy Shipping service file
      ansible.builtin.copy:
        src: shipping.service
        dest: /etc/systemd/system/shipping.service

    - name: Copy mongoDB repo file
      ansible.builtin.copy:
        src: mongo.repo
        dest: /etc/yum.repos.d/mongo.repo

    - name: Add roboshop application user
      ansible.builtin.user:
        name: roboshop

    - name: Create app directory
      ansible.builtin.file:
        path: /app
        state: directory

    - name: download and extract app content
      ansible.builtin.unarchive:
        src: https://roboshop-artifacts.s3.amazonaws.com/shipping-v3.zip
        dest: /app
        remote_src: yes

    - name: Install maven dependencies
      ansible.builtin.shell: mvn clean package ; mv target/shipping-1.0.jar shipping.jar
      args:
        chdir: /app/

    - name: Install MySQL Client
      ansible.builtin.dnf:
        name: mysql
        state: present

    - name: Load schema
      ansible.builtin.shell: mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/schema.sql

    - name: Load user
      ansible.builtin.shell: mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/app-user.sql

    - name: Load master data
      ansible.builtin.shell: mysql -h mysql-dev.adityadevops.online -uroot -pRoboShop@1 < /app/db/master-data.sql

    - name: Start shipping service
      ansible.builtin.systemd_service:
        state: restarted
        name: shipping
        enabled: true
        daemon_reload: true